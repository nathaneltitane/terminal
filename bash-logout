#!/bin/bash

# dextop ///////////////////////////////////////////////////////////////////////

if [ -f "${HOME}"/.dextop/dextop ]
then
	# automatic vnc session stop

	if [[ $(pidof Xvnc) ]]
	then
		container-session -x
	fi
fi
